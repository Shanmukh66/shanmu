1>git config --global user.name""

2>cat~/.git config.
                                                                                   [vim filename.extension]
3>git status

4>git add.     or    git add textname

5>git commit -m"valid message"

6>git push origin main or master

7>git pull origin main  (pull the element)